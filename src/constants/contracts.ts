export const CONTRACT_NAMES = Object.freeze({
  multisend: 'multisend',
  accessTokens: 'nova-access-tokens',
} as const);

export const STX_DECIMALS = 6;
export const MICRO_STX_PER_STX = 1_000_000;
export const MAX_MEMO_LENGTH = 34;

export type {};
